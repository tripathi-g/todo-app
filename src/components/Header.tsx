import React from "react";

const Header: React.FC = () => {
  return (
    <h1 className="py-8 flex justify-center items-center text-3xl font-bold">
      Task Nest
    </h1>
  );
};

export default Header;
